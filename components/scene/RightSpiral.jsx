import { styled } from '../../stitches.config';

const RightSpiral = styled('div', {
	height: '1440px',
	backgroundImage: 'url(./layers/spiral-right.svg)',
	animationDuration: '24s',
});

export default RightSpiral;
