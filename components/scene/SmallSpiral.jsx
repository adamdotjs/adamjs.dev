import { styled } from '../../stitches.config';

const SmallSpiral = styled('div', {
	height: '1440px',
	backgroundImage: 'url(./layers/spiral-sm.svg)',
	animationDuration: '25s',
});

export default SmallSpiral;
