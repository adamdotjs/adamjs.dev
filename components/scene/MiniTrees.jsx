import { styled } from '../../stitches.config';

const MiniTrees = styled('div', {
	zIndex: '1',
	bottom: '-80px',
	height: '400px',
	backgroundImage: 'url(./layers/mini-trees.svg)',
	animationDuration: '22s',
});

export default MiniTrees;
