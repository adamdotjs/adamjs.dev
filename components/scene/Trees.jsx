import { styled } from '../../stitches.config';

const Trees = styled('div', {
	zIndex: '2',
	bottom: '-550px',
	height: '1300px',
	backgroundImage: 'url(./layers/trees.svg)',
	animationDuration: '20s',
});

export default Trees;
