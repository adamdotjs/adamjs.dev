import { styled } from '../../stitches.config';

const LeftSpiral = styled('div', {
	height: '1440px',
	backgroundImage: 'url(./layers/spiral-left.svg)',
	animationDuration: '30s',
});

export default LeftSpiral;
