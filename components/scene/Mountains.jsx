import { styled } from '../../stitches.config';

const Mountains = styled('div', {
	bottom: '0',
	height: '600px',
	backgroundImage: 'url(./layers/mountains.svg)',
	animationDuration: '30s',
});

export default Mountains;
